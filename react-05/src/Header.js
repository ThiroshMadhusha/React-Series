import React from "react";

const Header = () => {
  return (
    // Inline css
    <header
      style={{
        backgroundColor: "mediumblue",
        color: "#fff",
      }}
    >
      <h1>This is Header</h1>
    </header>
  );
};

export default Header;
